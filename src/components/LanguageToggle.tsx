import React from 'react';
import { useTranslation } from 'react-i18next';
import { Languages } from 'lucide-react';

// تم إخفاء زر تبديل اللغة بناءً على طلب المستخدم
export const LanguageToggle: React.FC = () => null;